# Runtime dependencies - pinned to the exact set verified in CI/local so the
# environment is reproducible locally, in Docker, and on Streamlit Cloud.
#
# PyTorch CPU wheels: keeps the Linux image small (no ~2 GB CUDA stack). torch
# arrives transitively via sentence-transformers; we pin the CPU build so free
# hosts don't OOM/timeout during install.
--extra-index-url https://download.pytorch.org/whl/cpu

streamlit==1.59.1

# --- LangChain (pinned as a set; 1.x moved the chain helpers, so mixing majors
#     breaks imports) ---
langchain==1.3.12
langchain-core==1.4.9
langchain-community==0.4.2
langchain-huggingface==1.2.2
langchain-openai==1.3.4
langchain-text-splitters==1.1.2

# --- vector store, embeddings, PDF ---
faiss-cpu==1.14.3
sentence-transformers==5.6.0
pypdf==6.14.2
huggingface-hub==1.23.0
python-dotenv==1.2.2

# CPU-only torch on Linux; normal wheel elsewhere (mac/Windows dev).
torch==2.13.0 ; platform_system != "Linux"
torch==2.13.0+cpu ; platform_system == "Linux"
